# Assignment 1 — Sorting Algorithms

A performance comparison between **Insertion Sort** and **Heap Sort** using random and already-sorted integer arrays.

## Concepts
- Insertion Sort
- Heap Sort and heapify
- Algorithmic performance
- `std::vector`
- Timing with `<chrono>`

## Notes
The original coursework accidentally generated random input for both benchmark sections. This cleaned version correctly benchmarks both random and sorted arrays and verifies that each algorithm produces sorted output.

The original assignment report is preserved in `docs/assignment-report.docx`.
